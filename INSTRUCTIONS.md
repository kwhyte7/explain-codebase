# Setup and Usage Guide

To use this script as a standard Python module (e.g., `python -m explain_codebase`), follow these steps:

## 1. Install Dependencies

First, ensure you have Python 3.8 or higher installed. Then, install the project dependencies using pip and the `pyproject.toml` file provided:

