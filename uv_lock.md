```markdown
**Function Summary:**  
The function `list_packages()` returns a list of package details, including `name`, `version`, `source`, and `hash` fields.  

**Function Inputs:**  
- `name` (string): Package name.  
- `version` (string): Package version.  
- `source` (string): Package source (e.g., PyPI).  
- `hash` (string): Package hash (e.g., SHA-256).  

**Function Outputs:**  
- A list of dictionaries, each containing `name`, `version`, `source`, and `hash`.  

**Usage:**  
Use this function to retrieve a list of package details for documentation purposes.  

**Documentation:**  
The MD file contains the function's documentation, specifying inputs, outputs, and usage.  
```  
The MD file contains the function's documentation, specifying inputs, outputs, and usage.  
```